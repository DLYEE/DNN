import IO
import re

def viterbi():
    inputData = {}
    outputData = {}
    keyOrder = []
    def recurrence(remainObs, preValue, preTrace):
        if len(remainObs) > 0:
            value = []
            trace = []
            for i in range(0,48):

    
    
    
    
    for i in range(0, 48):
        viterbiLayer.append(viterbiNode([],1))
    for key in keyOrder:
        s = re.split("_| |\n", key)
        if s[:2] == currentSentenceName:
            sentence.append(key)
        else:
            currentSentenceName = s[:2]
            sentence = [key]
                     






